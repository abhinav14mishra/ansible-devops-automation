---
- name: Demonstrate service management
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Ensure cron service is running
      ansible.builtin.service:
        name: cron
        state: started
        enabled: true
