---
- name: EC2 Web Server Setup
  hosts: all
  gather_facts: false
  tasks:
    - name: Demonstrate EC2 Web Server Setup
      ansible.builtin.debug:
        msg: "Implement the EC2 Web Server Setup scenario with idempotent Ansible modules."
