---
- name: EC2 Application Deployment
  hosts: all
  gather_facts: false
  tasks:
    - name: Demonstrate EC2 Application Deployment
      ansible.builtin.debug:
        msg: "Implement the EC2 Application Deployment scenario with idempotent Ansible modules."
